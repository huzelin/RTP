syntax = "proto3";

package leader;

// The Ping service definition.
service PingService {
  rpc Ping (Request) returns (Reply) {}
}

message Request {
  uint32 id = 1;
}

message Reply {
  uint32 id = 1;
}
