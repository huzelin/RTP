package common;

message ItemConfig {
    required string id            = 1;
    required string version       = 2;
}

message VersionConfig {
    repeated ItemVersionConfig  item_version_config   = 1;
}
